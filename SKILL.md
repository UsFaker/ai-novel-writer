---
name: ai-novel-writer
description: Use when writing, continuing, revising, reviewing, outlining, or tracking long-form Chinese fiction projects, including requests like 写下一章, 继续写, 修改这段, 审查润色, 更新状态, 设计角色, or 写大纲.
---

# AI 小说创作工作流（自主执行引擎）

> **本文件是 AI 的执行程序，不是操作手册。收到任何写作指令后，必须按本文件的流程逐步执行。**

---

## Part 1: 指令路由

收到用户指令后，先判定类型再执行：

| 指令类型 | 识别方式 | 执行动作 |
|---------|---------|---------|
| **写章节**（"写下一章""写第X章""继续写""接着写"） | 含"写/继续/接着/生成" + "章" | → Part 2 单章闭环 |
| **写多章**（"写1-4章""写三章""批量"） | 含多章范围 | → Part 2 逐章串行，每章走完闭环再写下一章 |
| **局部修改**（"改一下这段""修改XX部分""调整对话"） | 含"改/修改/调整" + 具体内容 | → Part 2 的 STEP 2（重新加载素材）→ 定点修改 → STEP 5 审查 |
| **审查/润色**（"审查""润色""改稿"） | 含"审查/润色/改" | → Part 2 的 STEP 5 |
| **更新追踪**（"更新状态""提取事实"） | 含"更新/提取/状态/事实" | → Part 2 的 STEP 6 |
| **风格/规范调整**（"去AI味""爽文""快节奏""甜文""古风""文学性""口语化"） | 含文风、类型读感或写作禁忌关键词 | → Part 3.5，先写入风格/规范约束，再进入相关写作流程 |
| **全书剧情设计**（"没书感""剧情机械""关键物品乱写""设定太早摊牌""一整本书"） | 含全书/书感/机械/关键物品/关键因素/资产/剧透/乱写 | → Part 3，优先执行故事宪法与关键资产权限系统 |
| **初始化**（"新建项目""设定概念""设计角色""写大纲"） | 含"概念/角色/大纲/蓝图/长篇/300万字/百万字" | → Part 3 |
| **其他模糊写作指令** | 兜底 | → Part 2 单章闭环 |

**铁律**：
- ❌ 任何情况下都禁止收到写作指令后直接输出小说正文
- ❌ 禁止跳过素材加载——包括"只改一小段"也必须先重新加载情绪节拍和角色状态
- ❌ 禁止自行编造大纲中不存在的内容
- ❌ 缺素材时禁止继续，必须停下通知用户
- ❌ 禁止“凭记忆”执行——每次都必须重新读取文件，不能依赖之前对话中的残留印象
- ❌ 百万字级长篇禁止只用 4-6 个全书大弧代替卷、剧情单元/小弧、章段批次和章节节拍
- ❌ 不再支持章节备份相关操作；用户提出此类请求时，说明该功能已移除

---

## Part 2: 单章执行流程（长篇连续闭环）

### STEP 1: 定位目标章节

1. 扫描项目中的 `chapters/` 和 `drafts/` 目录
2. 只把正文章节文件计入编号：文件名需匹配 `第X章.md`、`chapter-X.md` 或 `chapter_X.md`
3. 排除 `方向_第X章.md`、`场景_*.md`、`scene_*.md`、`notes_*.md`、`backup_*` 等非正文文件
4. 优先以 `chapters/` 中的终稿编号为准；如果 `chapters/` 为空，再使用 `drafts/` 中的正文草稿编号
5. 找到最新已完成章节编号 N
6. 目标章节 = N+1（或用户指定的章节号）
7. 如果用户说"写下一章"且找不到任何已有章节，目标 = 第1章

### STEP 2: 加载素材（全部读完再写任何内容）

#### STEP 2.0: 写前上下文清单（防长篇失焦）

每次写作、修改、审查或生成下一章方向前，必须先重新读取并内部确认上下文。不要依赖前文对话记忆。

**必需项：缺失则停止并告诉用户缺什么**
1. `concept.md` 或文件名含"概念/concept"的故事概念文件
2. `outlines/` 中目标章节对应的情绪节拍/大纲行（含前后窗口）
3. 第2章起：角色状态表、累积事实清单、目标章节方向文档
4. 本章出场角色的 `characters/{角色名}.md`
5. 上一章正文末尾 2-3 段（第1章除外）
6. 百万字级长篇：`outlines/全书故事宪法.md`、`outlines/全书关键资产库.md`、`outlines/长篇结构索引.md`

**增强项：存在则必须读取，缺失只提醒不阻止**
1. `outlines/` 中的蓝图、弧线、时间线、关系追踪、伏笔追踪文件
2. `style.md`、`voice.md`、`requirements.md` 或文件名含"风格/规范/禁忌/去AI"的文档
3. 最近 3-5 章的章节标题、结尾状态和未回收钩子

内部加载后提取一份不输出的摘要：创作原则、目标章节、活跃角色、关系变化、必须推进的情节线、不可违背事实、关键资产权限、待回收伏笔、风格/规范约束。

按以下顺序读取文件，每项读取后提取关键信息：

**2a. 情绪节拍表（扩展阅读）**
→ 读取 `outlines/` 目录中文件名含"情绪节拍"的文件
→ 找到目标章节对应的行，**同时阅读前后各 10 章的节拍行**（共约 21 章窗口）
→ 从目标章节行提取：**情节目标**、**核心事件**、**出场角色**、**推进叙事线**、**主导情绪**、**情绪强度**、**情绪走向**、**章末钩子**
→ 从前 10 章了解：已发生的关键事件、已埋设的伏笔、情绪走势
→ 从后 10 章了解：即将发生什么、本章需要为后续铺垫什么
→ ❌ 找不到目标章节？先读取 `outlines/长篇结构索引.md`（如存在）：
   - 找到目标章节所属的宏观阶段/篇、卷、剧情单元/小弧和章段批次
   - 如果索引中已有该批次但缺情绪节拍，停止正文写作，通知用户先执行阶段3，为该批次生成情绪节拍
   - 如果索引也缺失，停止正文写作，通知用户先执行阶段0A（使用 `prompts/00a_longform_structure_index.md`）补长篇结构索引
→ ❌ 禁止在缺少目标章节节拍时自动跳到下一卷、下一阶段或自行生成正文

**2b. 角色状态表**（第2章起强制）
→ 读取 `outlines/` 目录中文件名含"角色状态"的文件
→ 找到 2a 中"出场角色"对应的状态卡片
→ 提取：**存活状态**、**当前位置**、**已知信息**、**不知道的关键信息**、**当前情绪**
→ ❌ 第2章起找不到或未更新到第N章？停止，通知用户先补充状态更新

**2c. 累积事实清单**（第2章起强制）
→ 读取 `outlines/` 目录中文件名含"事实清单"或"事实"的文件
→ 提取：**全局硬约束**（生死事实、物品归属）+ **近5章事实** + **待回收伏笔**
→ ❌ 第2章起找不到？停止，通知用户先补充事实提取

**2d. 角色档案**
→ 从 2a 获取出场角色名单
→ 读取 `characters/` 目录下对应角色名的 `.md` 文件
→ 提取：**核心性格**、**触发机制**、**对话风格**、**行为惯性**
→ ❌ 找不到角色文件？停止，通知用户先执行阶段2（使用 `prompts/02_character_design.md`）

**2e. 前情回顾**
→ 读取 `chapters/` 或 `drafts/` 中上一章文件的最后 2-3 段
→ 第1章跳过此步

**2f. 故事概念**
→ 读取项目根目录的 `concept.md`（或文件名含"概念/concept"的文件）
→ 提取：核心设定、世界观约束
→ ❌ 找不到？停止，通知用户先执行阶段1（使用 `prompts/01_story_concept.md`）

**2g. 全书故事宪法**（百万字级长篇强制）
→ 读取 `outlines/全书故事宪法.md`
→ 提取：全书终极命题、主角错误信念、世界核心矛盾、对立面哲学、全书不可逆变量、当前卷命运递进
→ ❌ 百万字级长篇找不到？停止生成新的卷纲、小弧、情绪节拍或正文，通知用户先执行阶段0B（使用 `prompts/00b_story_constitution.md`）

**2h. 关键资产权限**（百万字级长篇强制；如存在则所有项目必须遵守）
→ 读取 `outlines/全书关键资产库.md`
→ 按 `rules/asset_permission_rules.md` 提取：本章涉及的关键物品、秘密、力量规则、反派计划、世界真相、主角身世、组织制度、地点机关、关系真相、终局钥匙
→ 确认每个资产的当前解锁阶段、允许出现方式、允许解释深度、持有权、谁知道/不知道、禁止提前暴露内容
→ ❌ 百万字级长篇找不到？停止生成新的卷纲、小弧、情绪节拍或正文，通知用户先执行阶段0C（使用 `prompts/00c_asset_permission_system.md`）
→ ❌ 如果目标章节试图使用未解锁资产、提前解释后期用途、让主角知道不该知道的真相，停止并要求先调整资产库或重排节拍

**2i. 长篇结构信息**（长篇模式，如有）
→ 读取 `outlines/` 中文件名含"蓝图"或"blueprint"的文件
→ 读取 `outlines/` 中文件名含"长篇结构索引"或"structure index"的文件
→ 提取：当前宏观阶段/篇、当前卷、当前剧情单元/小弧、当前章段批次、卷目标、小弧冲突闭环、活跃故事线事件列表、当前卷必须改变的不可逆变量、当前卷允许解锁的关键资产
→ 如果项目预计 100 万字以上，但没有 `长篇结构索引.md`，停止生成新的情绪节拍或跨批次方向，通知用户先执行阶段0A

**2j. 下一章写作方向**（第2章起执行）
→ 读取 `drafts/` 目录中文件名含"方向_第X章"的文件（X为目标章节）
→ 提取：**情节目标**、**核心事件**、**出场角色**、**情绪走向**等完整约束要求
→ 作为本章核心写作锚点
→ ❌ 第2章起找不到？停止，通知用户先补齐生成方向文档

#### ✅ STEP 2 检查点（内部强制确认）

**⚠️ 在内部逐项确认以下信息，必须直接引用情绪节拍表原文，禁止自行概括或改写。**

1. 情节目标：「引用情绪节拍表原文」
2. 核心事件：「引用情绪节拍表原文」
3. 出场角色：A（对话风格：XX）、B（对话风格：XX）、...
4. 情绪走向：从「引用原文」→ 经过「引用原文」→ 到达「引用原文」
5. 章末钩子：「引用原文」
6. 禁止事项：（已死亡角色、信息差约束等）
7. 关键资产权限：本章涉及资产、当前解锁阶段、允许写到的深度、禁止提前暴露内容
8. 全书宪法对齐：本章是否服务当前卷命运递进和至少一个不可逆变量；若完全不服务，标记为弱章并停止进入正文

→ 如果任何一项无法填写（文件不存在或缺少信息），**停止执行**，通知用户缺少什么素材。
→ 全部确认后直接进入 STEP 2.5。

### STEP 2.5: 建立叙事视角契约（内部强制确认）

→ **资料只作为约束，不是正文素材。** 情绪节拍表、方向文档、角色状态表和事实清单只决定“能写什么/不能写什么/必须发生什么”，不能决定正文句子怎么写。除必须保留的关键台词、专名和章末钩子外，禁止直接沿用这些资料里的表达、比喻、句式和段落顺序。

写正文前必须在内部确定以下契约，不输出：

1. **主视角角色**：本章每个场景中，读者站在谁身后/谁的感知里看事。
2. **叙事距离**：贴身（进入感知和即时念头）/中距（只写可观察动作和少量心理）/远距（只用于转场或宏观信息），每个场景只能选一个主距离。
3. **读者问题**：场景开头让读者想知道什么；场景结尾回答什么、留下什么。
4. **场景发动机**：视角角色想要什么、遇到什么阻碍、做出什么选择、付出什么代价。
5. **信息释放顺序**：作者知道但视角角色不知道的信息，禁止提前进入旁白；只能通过可见动作、台词、误判和后果让读者逐步意识到。
6. **视角切换规则**：双线或多视角章节必须用分隔符切换；同一场景内禁止无标记跳进另一个角色的内心。

→ 全部确认后，再进入 STEP 2.6。

### STEP 2.6: 建立创作判断层（内部强制确认）

→ **人物先于资料。** 写正文前必须把 STEP 2 的所有资料转化为“场内人物会怎样想、怎样误判、怎样遮掩、怎样行动”。资料不能绕过人物判断直接进入正文。

→ **设定只能改变行为，不能替正文解释行为。** 例如“角色不知道真相”不能写成旁白说明“他不能说，因为她还不知道”，必须转化为：他改口、沉默、反问、岔开话题、误导、被追问、付出关系代价等场内行为。

每个关键场景必须在内部建立以下判断，不输出：

1. **表层目标**：视角角色此刻明面上想完成什么。
2. **真实动机**：他/她不愿承认、不能明说或自己也没想清楚的欲望。
3. **压力来源**：对方、环境、时间、秘密、身份、代价中，哪一项正在逼迫角色。
4. **误判与盲区**：角色基于当前已知信息会误会什么、低估什么、看漏什么。
5. **遮掩策略**：角色会用什么方式避开真话：反问、转移、强硬、玩笑、沉默、行动代替回答、故意说一半。
6. **设定转行为**：本场涉及的每条硬设定/信息差/性格标签，必须对应一个动作、台词、沉默、选择或后果。
7. **不可逆变化**：本场结束时，局势、关系、信息或行动比入场时多了哪一个变化。

→ 如果某条设定只能写成解释句，说明还没有完成创作判断，必须继续转化，直到它能落到场内行为。
→ 全部确认后，再进入 STEP 3。

### STEP 3: 生成场景骨架与关键对话素材

→ 角色定位：你是一位擅长写对话的小说作家
→ 按 `rules/writing_rules.md` 中的**对话规则**
→ 为本章 2-3 个关键场景生成**工程稿**，不是正文初稿
→ 每个场景必须包含：视角角色、读者问题、入场状态、表层目标、真实动机、压力来源、误判/盲区、遮掩策略、冲突阻碍、角色选择、结果/代价、续场反应（情绪→困境→决定）、必写台词、潜台词、禁止泄露的信息
→ 对话必须服务于 STEP 2 确定的**情节目标**、STEP 2.5 的**叙事视角契约**和 STEP 2.6 的**创作判断层**
→ 角色说话方式必须符合 STEP 2 加载的**对话风格**
→ 角色不能泄露状态表中**不知道的关键信息**
→ 禁止在场景素材中写“因为设定/状态表/大纲要求所以角色……”这类解释；只能写角色当场会做什么、说什么、避开什么、误判什么
→ 禁止把场景素材写成完整小说段落；禁止在场景素材中写可直接复制进正文的氛围句、比喻句、结尾句
→ 保存到 `scenes/` 目录

### STEP 4: 生成章节正文

→ 角色定位：你是一位经验丰富的网络小说作家
→ 综合 STEP 2 全部素材 + STEP 2.5 叙事视角契约 + STEP 2.6 创作判断层 + STEP 3 场景骨架与关键对话
→ 从叙事视角契约和创作判断层出发重新起草正文，禁止把 STEP 3 工程稿当作半成品正文扩写
→ 按 `rules/writing_rules.md` 中的**小说感与叙事视角**、**情绪执行规则**、**禁止事项**和**必须事项**
→ 严格遵守 `rules/writing_rules.md` 中的**禁止事项**和**必须事项**
→ 每条角色设定、信息差和事实约束都必须转化为场内动作、台词、沉默、误判、选择或后果，禁止用旁白解释“因为设定如此”
→ 每段至少承担一个叙事功能：场内感知、动作、台词、局势变化、信息误判、后果落地；不承担功能的资料说明必须删除
→ 用“读者问题”控制信息释放：先让读者在场内产生疑问，再用行动、台词或结果给答案，禁止用旁白直接替资料做解释
→ 字数目标：2000-3000 字（优先保证节奏，不凑字数）
→ 保存到 `drafts/` 目录

### STEP 5: 审查润色

→ 角色定位：你是一位严格的小说编辑
→ 按 `rules/review_rules.md` 逐项执行：
   1. **一致性审查**：对照角色状态表和事实清单，逐项核验
   2. **小说感与视角审查**：检查是否有清晰主视角、读者问题、场内行动和资料扩写痕迹
   3. **创作判断审查**：检查设定是否转化为人物欲望、误判、遮掩、选择和后果，删除设定泄漏
   4. **去 AI 化**：按高频词表和排版规则检查
   5. **润色**：按润色铁律修改
   6. **校对**：错别字和语病
→ 🔴硬伤必须修复后再输出
→ 保存终稿到 `chapters/` 目录

### STEP 6: 更新追踪文件

→ 按 `rules/tracking_rules.md` 的格式执行两项更新：

**6a. 更新角色状态表**
→ 读取 `outlines/` 中文件名含"角色状态"的文件
→ 为本章登场角色更新状态卡片
→ 未登场角色沿用上一版
→ 标注死亡/新增/关系变化/信息差
→ 保存回原文件

**6b. 追加事实清单**
→ 读取 `outlines/` 中文件名含"事实"的文件
→ 从本章终稿提取硬事实（生死/物品/身份/信息/伏笔/时间线）
→ 追加到文件末尾（只增不改）
→ 保存回原文件

**6c. 更新长期追踪索引（如存在）**
→ 如果存在弧线/伏笔/时间线/关系追踪文件，更新：
   1. 角色最后出场章节
   2. 活跃情节线最新推进章节与当前状态
   3. 新埋伏笔、已回收伏笔、超过阈值未回收伏笔
   4. 时间跨度、地点移动、关系重大变化
→ 如果发现角色 10+ 章未出现、情节线 12+ 章无进展、伏笔 20+ 章未回收，在 STEP 7 简报中提醒用户

### STEP 6.5: 生成下一章方向草稿

→ 角色定位：你是一位负责大纲统筹的主编
→ 按 `rules/next_chapter_direction_rules.md` 执行：
   1. 提取情绪节拍表中第 N+1 章的要求
   2. 读取长篇结构索引，确认第 N+1 章所属的卷、剧情单元/小弧和章段批次
   3. 结合本章终稿的结尾状态（承接点、未收钩子）
   4. 结合最新状态表和事实清单设定下一章的硬约束
→ 如果第 N+1 章没有情绪节拍，停止生成方向文档，提示先补当前章段批次的情绪节拍；不要改写成下一卷/下一阶段方向
→ 生成一份方向规范，保存到 `drafts/方向_第{N+1}章.md`

### STEP 7: 报告完成

向用户简报：
- ✅ 第X章已完成
- 本章情节：（1句话概括情节目标达成情况）
- 本章字数：X字
- 关键事件：（列出1-2个关键事实）
- 状态更新：（标注死亡/新增角色/重大关系变化，无则省略）
- 下章方向：已生成 `drafts/方向_第{N+1}章.md`

---

## Part 2 补充：批量章节规则

写多章时：
1. 每章串行走完 STEP 1-7 后才开始下一章
2. 下一章的 STEP 2 必须使用本章 STEP 6 更新后的最新状态表和事实清单
3. 每章完成后向用户简报（STEP 7），再继续下一章
4. 禁止先批量生成所有初稿再统一审查

---

## Part 3: 初始化流程

以下任务是一次性/低频执行的，使用对应的 prompt 模板文件引导用户完成：

| 任务 | 使用文件 | 产出保存位置 |
|------|---------|------------|
| 故事概念 | `prompts/01_story_concept.md` | `concept.md` |
| 全书故事宪法（百万字级强制） | `prompts/00b_story_constitution.md` | `outlines/全书故事宪法.md` |
| 全书蓝图（长篇宏观方向） | `prompts/00_story_blueprint.md` | `outlines/全书蓝图.md` |
| 全书关键资产库（百万字级强制） | `prompts/00c_asset_permission_system.md` | `outlines/全书关键资产库.md` |
| 长篇结构索引（百万字级强制） | `prompts/00a_longform_structure_index.md` | `outlines/长篇结构索引.md` |
| 角色设计 | `prompts/02_character_design.md` | `characters/{角色名}.md` |
| 情绪节拍大纲 | `prompts/03_emotion_outline.md` | `outlines/情绪节拍_第X卷.md` |

执行顺序：概念 → 全书故事宪法 → 蓝图（长篇宏观方向）→ 全书关键资产库 → 长篇结构索引（卷/小弧/批次）→ 角色 → 大纲约束卡 → 情绪节拍 → 即可进入 Part 2 写章节

### 长篇结构层级（百万字级强制）

百万字级项目必须按以下层级组织：

```
全书终局/主题
  └── 全书故事宪法（命题、信念、不可逆变量）
        └── 全书关键资产库（物品、秘密、规则、真相的解锁权限）
              └── 宏观阶段/篇（几十万字级，只管大方向）
                    └── 卷（约 40-100 章，管阶段任务和卷末状态）
                          └── 剧情单元/小弧（约 8-30 章，管一个冲突闭环）
                                └── 章段批次（建议 10-20 章，管连续节拍生成）
                                      └── 章节节拍（单章目标、行动、变化、钩子）
```

**铁律**：
- “弧”默认指卷内的剧情单元/小弧；如果用户说“5个大弧”，只可理解为 5 个宏观阶段/篇，不能直接等于 5 卷或完整章纲。
- 全书蓝图不负责列完所有章节；长篇结构索引负责把全书拆到卷、小弧和章段批次。
- 全书故事宪法负责“为什么是一本书”；关键资产库负责“什么东西什么时候才允许出现和解释”。
- 情绪节拍按章段批次生成。写到第 N 章缺节拍时，先补当前卷/当前小弧的下一批次，不得自行跨到下一阶段。
- 每个剧情单元/小弧必须有“起点问题 → 中段转折 → 终点结果”；只有情绪强度递增不算剧情规划。
- 关键物品、核心秘密、世界真相和终局钥匙必须按资产库解锁，禁止因为素材里有就提前写完。

---

## Part 3.5: 风格与规范探测

当用户提出文风、类型读感或写作禁忌时，先把需求转成可执行约束，再写作。

### 常见触发
- **去 AI 味/自然/口语化/像人写的**：减少模板词、抽象总结和机械对仗，优先具体动作、短句、口语化判断。
- **爽文/快节奏/打脸/升级**：提高动作和结果密度，每章至少有明确推进、反击、获得或代价。
- **无毒点/不降智/逻辑自洽**：禁止强行误会、工具人行为、信息只为拖剧情而隐藏。
- **甜文/撒糖/HE**：控制冲突烈度，糖点必须落到行动、选择和关系推进。
- **虐文/BE/刀子**：虐点必须有因果和代价，禁止为虐而降智。
- **文学性/严肃/克制/极简/古风**：调整句式、意象和节奏，但不得牺牲场内行动和读者问题。

### 执行规则
1. 先判断用户是临时要求还是项目长期规范。
2. 临时要求：只应用到当前任务，并在 STEP 2.0 内部摘要中记录。
3. 长期规范：建议写入 `style.md` 或 `requirements.md`；用户同意后再创建/修改文件。
4. 多个规范冲突时，先说明冲突并给出优先级。例如"快节奏爽文"优先直给推进，"严肃文学"优先克制留白，不能同时把同一场戏写成满糖快爽和冷硬极简。
5. 已有项目风格文件优先于临时猜测；不得凭关键词覆盖用户已有设定。

---

## Part 4: 长篇检查点

### 卷末检查（每 20-50 章）
使用 `prompts/09_arc_subplot_tracker.md` 的卷末追踪模板执行：
1. 更新叙事线进展表
2. 更新故事线事件追踪表（伏笔/推进/转折/高潮/收束）
3. 检查当前卷内剧情单元/小弧是否完成冲突闭环
4. 叙事线健康检查（有无线连续2卷潜伏）
5. 检查本卷是否改变了全书故事宪法中的至少 2 个不可逆变量
6. 检查关键资产是否按权限解锁，有无提前剧透、无代价揭示、物品机械救场
7. 确认下一卷角色批次与资产解锁窗口
8. 为下一卷或下一章段批次执行阶段3（情绪节拍）
9. 检查遗忘元素：10+ 章未出场角色、12+ 章无推进情节线、20+ 章未回收伏笔

### 弧末检查（每 50-150 章）
使用 `prompts/09_arc_subplot_tracker.md` 的弧末清算模板执行：
1. 弧级总结评分
2. 蓝图偏差分析
3. 评估是否需要修订后续弧
4. 输出一致性风险清单：角色知识、世界规则、时间线、关系状态、伏笔债务

---

## 项目目录结构

```
my-novel/
├── concept.md              # 故事概念
├── characters/             # 角色档案（每角色一个文件）
├── outlines/               # 大纲与追踪文档
│   ├── 全书故事宪法.md     #   全书命题、信念递进、不可逆变量
│   ├── 全书关键资产库.md   #   关键物品/秘密/规则/真相的解锁权限
│   ├── 情绪节拍_第X卷.md   #   情绪节拍表
│   ├── 长篇结构索引.md     #   阶段/卷/小弧/批次结构
│   ├── 角色状态表.md       #   动态角色状态（每章更新）
│   └── 累积事实清单.md     #   事实清单（只增不改）
├── scenes/                 # 场景对话素材
├── drafts/                 # 章节初稿与方向草稿（方向_第X章.md）
└── chapters/               # 终稿
```

---

## 参考文件

- `rules/writing_rules.md` — 写作规则（对话 + 正文 + 禁止/必须事项）
- `rules/review_rules.md` — 审查润色规则
- `rules/tracking_rules.md` — 状态和事实追踪格式
- `rules/asset_permission_rules.md` — 关键物品、秘密、规则、真相的出场与解锁权限
- `rules/next_chapter_direction_rules.md` — 下一章写作方向生成规则
- `prompts/00-03` — 初始化模板（含 00A 长篇结构索引、00B 全书故事宪法、00C 关键资产权限系统）
- `prompts/09` — 弧/支线追踪模板
- `examples/` — 工作流示例和角色档案示例
